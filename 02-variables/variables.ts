// let variableName: type = value
let greetings: string = "Hello Delon";

// cant assign the variable to another type since we declared the type as string
// greetings = 6;
// greetings = true;

greetings.toLowerCase();
console.log(greetings);

// Sometimes you dont need to declare the type like : type
let myNum = 6;
// this property doesnt exist on the type number (only strings)
// myNum.toUpperCase();

export {};
