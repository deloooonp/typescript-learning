const user = { name: "Delon", age: 10 };

console.log("Delon");

console.log(user.name);

// .email will give an error because email property doesnt exist
// tsc will still work but the code wont run in javascript
// console.log(user.email);
